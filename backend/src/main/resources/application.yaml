spring:
  application:
    name: backend
  profiles:
    active: ${SPRING_PROFILE_ACTIVE:dev}
  datasource:
    url: jdbc:mariadb://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME}?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver
  mybatis:
    mapper-locations: classpath*:mapper/**/*.xml
    type-aliases-package: com.yellowpang.backend
    configuration:
      map-underscore-to-camel-case: true
      jdbc-type-for-null: null
      default-enum-type-handler: org.apache.ibatis.type.EnumTypeHandler
  flyway:
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0

logging:
  level:
    root: INFO
    com.yellowpang.backend: INFO
    org.springframework.security: WARN
    org.mybatis: INFO
